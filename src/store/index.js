import { createStore } from 'vuex';
import banks from '@/store/modules/banks';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    banks
  }
})
